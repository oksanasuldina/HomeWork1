digit = input('Введите целое положительное число: ')
# в начале алгоритма за самое число принимаем первое в строке, то есть с индексом 0
maxNumber = int(digit[0])  # сразу переводим в тип int для выполнения арифметических операций
# определяем длину строки для ограничения цикла
lengthStr = len(digit)
# так как число с индексом 0 принято за самое большое, анализируем со следующего, с индекса 1
i = 1
while i < lengthStr:  # пока индекс числа строки меньше длины
    # (именно меньше так как максимальный индекс на 1 меньше длины строки)
    if int(digit[i]) > maxNumber:  # если анализируемое число больше, то запоминаем
        maxNumber = int(digit[i])
        if maxNumber != 9:
            i += 1
        else:
            break  # если нашли 9, выходим
    else:  # если нет, просто идем дальше
        i += 1
print('Самая большая цифра в числе: {}'.format(maxNumber))
